## Behaviour Change

<!-- One concrete paragraph: what the request actually changes in the system.
     Not a restatement of the words — name the specific component behaviour,
     data shape, or contract that will be different after the work. -->

## Relevant Architecture

<!-- The existing components, packages, interfaces, and integration points that
     this change touches or depends on. Name only what you confirmed with
     Read/Grep/Glob — no inventory of files you didn't open. -->

## Files Likely to Change

<!-- Bullet list of file paths confirmed by reading, with a one-line note on why
     each one is in scope. If you couldn't confirm a file exists, don't list it. -->

## Repo Standards

<!-- Coding, architecture, and testing patterns this change must follow — pulled
     from what you actually found in the codebase, not from general knowledge.
     These feed directly into the spec's Constraints section. -->

## SDK / Dependency Coupling

<!-- Where an existing external dependency (SDK, library, framework) creates a
     seam, constraint, or design tension for this feature. Leave blank if none. -->

## Open Design Questions

<!-- Questions the spec must resolve that you can't answer from the codebase
     alone — ambiguous interfaces, missing contracts, competing approaches. -->

## Assumptions

<!-- What you assumed to be true in order to produce the above. Flag anything
     a reviewer should verify before the spec is written. -->
