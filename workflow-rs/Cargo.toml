[package]
name = "jig-workflow"
version = "0.1.0"
edition = "2024"
description = "Rust port of jig's internal/workflow package"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
toml = "0.9"

[dev-dependencies]
tempfile = "3"

